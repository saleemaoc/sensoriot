package com.edexelroots.android.sensoriot.vision.views;

import android.content.Context;
import android.graphics.Color;
import android.util.AttributeSet;
import android.widget.TextView;

import com.edexelroots.android.sensoriot.R;

public class ShimmerText /*extends ShimmerLayout*/ {
/*
    public TextView header;
    public ShimmerText(Context context) {
        this(context, null);
        init();
    }

    public ShimmerText(Context context, AttributeSet attrs) {
        super(context, attrs);
        init();
    }

    private void init() {
        inflate(getContext(), R.layout.text_view, this);
        this.header = findViewById(R.id.text_view);
        if(this.header.getParent() == null) {
            addView(this.header);
        }
    }

    public void setText(String text) {
        this.header.setText(text);
    }

    public CharSequence getText(){
        return this.header.getText();
    }*/
}
